run("Subtract Background...", "rolling=12");
run("8-bit");
setAutoThreshold("Default dark");
//run("Threshold...");
//setThreshold(24, 255);
setOption("BlackBackground", false);
run("Convert to Mask");
run("Close");
run("Fill Holes");
run("Convert to Mask");
run("Watershed");
run("Set Measurements...", "area mean min centroid redirect=None decimal=3");
run("Analyze Particles...", "size=8-Infinity show=Outlines display exclude clear summarize");
